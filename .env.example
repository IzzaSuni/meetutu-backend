# Copy to .env and fill in. Never commit the filled-in file.

# --- required ---------------------------------------------------------------
# Single-user login for the meetutu frontend.
AUTH_USERNAME=admin
AUTH_PASSWORD=change-me
# Google Generative Language API key (https://aistudio.google.com/apikey).
GEMINI_API_KEY=

# --- optional ---------------------------------------------------------------
PORT=8787
HOST=0.0.0.0
# SQLite file and audio parts live here. Back this directory up.
DATA_DIR=./data
GEMINI_API_URL=https://generativelanguage.googleapis.com/v1beta
GEMINI_MODEL=gemini-3.6-flash
# Only when GEMINI_API_URL points at a Cloudflare AI Gateway.
CF_AIG_TOKEN=
# Alternative provider, used when a request asks for X-AI-Provider: openrouter.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=anthropic/claude-3.5-haiku
# Comma-separated allowed origins. Narrow this to the frontend origin in production.
CORS_ORIGINS=*
