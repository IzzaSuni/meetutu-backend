# pnpm 12 reads build-script approvals from here, not from package.json.
# better-sqlite3 compiles (or downloads) its native binding; esbuild installs
# the platform binary vitest runs on.
allowBuilds:
  better-sqlite3: true
  esbuild: true
